class Game {
    constructor(thisPlayerIsPlayerAsWhite) {
        this.thisPlayerIsPlayerAsWhite = thisPlayerIsPlayerAsWhite // boolean
    }

    makeStartingBoard() {
        // make the board orientation for the current player
        if (this.thisPlayerIsPlayerAsWhite) {
            // make chess orientation for white
        } else {

        }
    }

    movePiece(pieceId, to) {
        // pieceId: string
        // to: [x, y]
    }
}