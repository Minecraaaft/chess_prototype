class Square {
    constructor(x, y, canvasCoordinate, piece) {
        this.x = x
        this.y = y
        this.canvasCoordinate = canvasCoordinate // in pixels
        this.piece = piece // chess piece
    }
}